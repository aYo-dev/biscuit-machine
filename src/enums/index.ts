export enum MachineStates {
    on = 'on',
    off = 'off',
    pause = 'pause',
  };
  
export enum BiscuitStates {
  raw = 'raw',
  stamped = 'stamped',
  bake = 'bake',
  baked = 'baked',
};

export enum BmListTypes {
  basket = 'basket',
  belt = 'belt',
};
